<Window x:Class="DimaCalc.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="64" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="1">
            <TextBox Grid.Row="1" DockPanel.Dock="Top" 
                     Name="textBox1" VerticalAlignment="Bottom" 
                     BorderBrush="Black" BorderThickness="1" Background="Silver"
                     MinHeight="40" MaxHeight="40"/>
        </DockPanel>
        <Button Content="Calculate" 
                HorizontalAlignment="Left" 
                Name="button1" Width="75" 
                Click="button1_Click" Margin="0,0,0,9" Height="23" VerticalAlignment="Bottom" />
    </Grid>
</Window>
